{% extends 'base.html.twig' %}

{% block title %}Edit Training{% endblock %}

{% block body %}
    <div class="container">
    <h1>Edit Training</h1>

    {{ include('training/_form.html.twig', {'button_label': 'Update'}) }}

        <button style="margin-top: 10px" type="button" class="btn btn-info"> <a style="text-decoration: none; color: white" href="{{ path('training_index') }}">back to list</a></button>

{#    {{ include('training/_delete_form.html.twig') }}#}
    </div>
{% endblock %}
