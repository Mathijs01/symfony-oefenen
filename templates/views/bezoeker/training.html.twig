{% extends 'base.html.twig' %}

{% block main %}
    <div class="container">
        <div class="row">
            <div class="col">
            {% for training in trainings %}
                <tr>{{ training.naam }}</tr> <br>
            {% endfor %}
            </div>
            <div class="col">
            {% for training in trainings %}
                <tr>{{ training.description }}</tr> <br>
            {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}