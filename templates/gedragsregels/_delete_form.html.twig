<form method="post" action="{{ path('gedragsregels_delete', {'id': gedragsregel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <div class="container">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gedragsregel.id) }}">
{#    <button class="btn">Delete</button>#}
        <button class="btn btn-danger" style="margin-top: 10px">Delete</button>
    </div>
</form>
